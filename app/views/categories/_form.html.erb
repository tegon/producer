<%= form_for(@category) do |f| %>
  <%= render "shared/error_messages", record: @category %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
